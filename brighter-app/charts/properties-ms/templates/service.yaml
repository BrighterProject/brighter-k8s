apiVersion: v1
kind: Service
metadata:
  name: properties-ms
  labels:
    {{- include "properties-ms.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "properties-ms.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
